server:
  port: 8080

security:
  jwt:
    public-key: classpath:public.txt

spring:
  jackson:
    serialization:
      INDENT_OUTPUT: true
  kafka:
    producer:
      bootstrap-servers: localhost:9092

order:
  topic: ordertopic